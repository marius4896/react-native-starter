import {StyleSheet} from "react-native";
import * as AppStyles from "src/theme";

const styles = StyleSheet.create({
    root: {
        ...AppStyles.Styles.screen.root
    }
});

export default styles;
